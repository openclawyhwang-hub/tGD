---
description: Review — five-axis code review before merging any change
---

Run the `code-review-and-quality` skill. Apply five-axis review with severity labels (Nit/Optional/FYI). Keep changes to ~100 lines. If the change is larger, split it.

Check for security concerns → run `security-and-hardening`.
Check for performance concerns → run `performance-optimization`.
