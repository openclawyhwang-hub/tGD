---
description: Build — implement one thin vertical slice at a time with feature flags
---

Run the `incremental-implementation` skill. Implement one thin vertical slice at a time: implement → test → verify → commit. Use feature flags for incomplete features, safe defaults, and rollback-friendly changes.

Apply `doubt-driven-development` to any non-trivial decision during implementation. Apply `context-engineering` to load the right context at each step.
