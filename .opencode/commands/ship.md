---
description: Ship — pre-launch checklists, staged rollouts, rollback procedures
---

Run the `shipping-and-launch` skill. Execute pre-launch checklist, verify feature flags, set up staged rollouts, confirm rollback procedures, and configure monitoring.

Also run `git-workflow-and-versioning` for clean commit history and `documentation-and-adrs` to document decisions.
