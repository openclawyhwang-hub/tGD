---
id: architecture
title: Architecture
sidebar_position: 3
---

import LayerBadge from '@site/src/components/LayerBadge';

# Architecture

## Summary

**{{ project.name }}** is organized into **{{ layers | length }}** architectural layer{{ 's' if layers | length != 1 else '' }}.

## Layers

{% if layers %}
| Layer | Description | Files |
|---|---|---:|
{% for l in layers %}| <LayerBadge label={{ l.name | tojson }} accent={{ l.accent | tojson }} /> **{{ l.name }}** | {{ l.description }} | {{ l.node_count }} |
{% endfor %}
{% else %}
_No layers were identified._
{% endif %}

## Architecture Diagram

```mermaid
{{ architecture_mermaid }}
```

## Dependency Overview

```mermaid
{{ dependency_mermaid }}
```

## Key Patterns

{% if patterns %}
{% for p in patterns %}
- **{{ p.name }}** — {{ p.description }}
{% endfor %}
{% else %}
_No recurring patterns extracted. This section is populated when the graph contains enough `similar_to` or `implements` edges._
{% endif %}

## Related Pages

- [Overview](/overview)
{% for m in modules %}
- [{{ m.title }} module](/modules/{{ m.slug }})
{% endfor %}
- [Home](/)
