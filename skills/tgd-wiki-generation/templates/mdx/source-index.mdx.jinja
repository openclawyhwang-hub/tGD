---
id: source-index
title: Source Browser
sidebar_position: 6
---

# Source Browser — {{ project.name }}

Offline source pages generated from local files when available. Symbol tables in module pages link here with `#L<line>` anchors when a line number can be inferred.

{% if source_files %}
| File | Lines | Available |
|---|---:|---|
{% for f in source_files %}| [`{{ f.path }}`]({{ base_path }}/source/{{ f.slug }}) | {{ f.line_count }} | {{ "✅" if f.available else "⚠️ source file unavailable" }} |
{% endfor %}
{% else %}
_No source files were discovered from the knowledge graph._
{% endif %}

---

**Related pages:** [Architecture]({{ base_path }}/architecture) · [{{ project.name }} Home]({{ base_path }}) · [Wiki Home](/)
