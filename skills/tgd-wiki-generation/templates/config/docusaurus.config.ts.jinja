import type {Config} from '@docusaurus/types';
import type * as Preset from '@docusaurus/preset-classic';

const config: Config = {
  title: {{ site_title | tojson }},
  tagline: {{ site_tagline | tojson }},
  favicon: 'img/favicon.ico',

  url: 'http://localhost',
  baseUrl: '/',
  organizationName: 'tgd',
  projectName: {{ project_name | tojson }},

  onBrokenLinks: 'warn',
  onBrokenMarkdownLinks: 'warn',

  i18n: {
    defaultLocale: 'en',
    locales: ['en'],
  },

  markdown: {
    mermaid: true,
  },

  themes: ['@docusaurus/theme-mermaid'],

  presets: [
    [
      'classic',
      {
        docs: {
          routeBasePath: '/',
          sidebarPath: './sidebars.ts',
        },
        blog: false,
        theme: {
          customCss: './src/css/custom.css',
        },
      } satisfies Preset.Options,
    ],
  ],

  themeConfig: {
    navbar: {
      title: {{ navbar_title | tojson }},
      items: [
        {to: '/', label: 'Home', position: 'left'},
        {% if multi_repo %}
        {
          type: 'dropdown',
          label: '📦 Repos',
          position: 'left',
          items: [
            {% for r in repos %}
            {to: '/repos/{{ r.slug }}', label: {{ (r.name ~ (" ⭐" if r.slug == primary_slug else "")) | tojson }}},
            {% endfor %}
          ],
        },
        {to: '/sources', label: 'All Sources', position: 'left'},
        {% else %}
        {to: '/repos/{{ primary_slug }}/overview', label: 'Overview', position: 'left'},
        {to: '/repos/{{ primary_slug }}/architecture', label: 'Architecture', position: 'left'},
        {to: '/repos/{{ primary_slug }}/onboarding', label: 'Onboarding', position: 'left'},
        {% endif %}
      ],
    },
    footer: {
      style: 'dark',
      copyright: 'Generated by tGD Wiki • CodeGraph + Understand-Anything',
    },
    prism: {
      theme: require('prism-react-renderer').themes.github,
      darkTheme: require('prism-react-renderer').themes.dracula,
    },
    colorMode: {
      defaultMode: 'light',
      respectPrefersColorScheme: true,
    },
    mermaid: {
      theme: {light: 'default', dark: 'dark'},
    },
  } satisfies Preset.ThemeConfig,
};

export default config;
