# External Jira Configuration (Jira Cloud)
# Copy to config.yaml and fill in your values

jira:
  url: "https://<your-domain>.atlassian.net"
  token: ""  # Store in env var: JIRA_TOKEN
  project_key: ""
